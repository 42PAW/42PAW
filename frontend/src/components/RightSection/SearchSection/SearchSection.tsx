import styled from "styled-components";

const SearchSection = () => {
  return <WrapperStyled>Helo</WrapperStyled>;
};

const WrapperStyled = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100%;
  width: 100%;
  overflow: scroll;
`;

export default SearchSection;
